package de.cses.client.kuchaMapPopupPanels;

import com.sencha.gxt.widget.core.client.tips.ToolTipConfig;

/*public class HilfePanels{
	Detailansicht detailansicht;
	Environment environment;
	
	ToolTipConfig editconfig = new ToolTipConfig();
	ToolTipConfig saveconfig = new ToolTipConfig();
	ToolTipConfig exitconfig = new ToolTipConfig();
	ToolTipConfig buttonsizeconfig = new ToolTipConfig();
	ToolTipConfig doubleclickconfig = new ToolTipConfig();
	ToolTipConfig loeschenconfig = new ToolTipConfig();
	

public void showPanels(){
	environment.getEdit().setToolTipConfig(editconfig);
	environment.getExit().setToolTipConfig(exitconfig);
	environment.getSave().setToolTipConfig(saveconfig);
	environment.getHoehlenButtonSlider().setToolTipConfig(buttonsizeconfig);
	
}


public void closePanels(){
	environment.getEdit().removeToolTip();
	environment.getExit().removeToolTip();
	environment.getSave().removeToolTip();
	environment.getHoehlenButtonSlider().removeToolTip();
	
}

	public  HilfePanels(){
		detailansicht = Home.getKuchaMapPrototyp().getDetailansicht();
		environment = Home.getKuchaMapPrototyp().getEnvironment();
	}
	public void initPanels(){
		
		
		editconfig.setTitle("Edit this Page");
		editconfig.setBody("Allows to create new caves, change cave size and change district map");
		
		saveconfig.setTitle("Save changes");
		saveconfig.setBody("Click to save all changes and leave edit view");
		
		exitconfig.setTitle("Exit edit view");
		exitconfig.setBody("Click to leav edit view, all changes will be undone.");
		
		buttonsizeconfig.setTitle("Cave size slider");
		buttonsizeconfig.setBody("Move the slider to change the cave size and adjust the caves to the map.");
		
		doubleclickconfig.setTitle("Click to create new caves");
		doubleclickconfig.setBody("Make a double-click on a position where you want to create a new cave.");
		
		loeschenconfig.setTitle("Delete caves");
		loeschenconfig.setBody("Make a double-click on a cave you want to delete.");
		
		
	}
	
}
*/
